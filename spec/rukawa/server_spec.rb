require 'spec_helper'

describe Rukawa::Server do
  it 'has a version number' do
    expect(Rukawa::Server::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
